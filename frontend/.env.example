# 前端环境变量示例（只放占位符，禁止填入真实密钥）
# 复制为 .env.local 使用。

# 后端 API 基地址。
# 留空时，开发服务器会把 /api 与 /actuator 代理到 http://localhost:8080（见 vite.config.ts）。
# 生产/演示环境填真实地址，例如：https://api.example.com
VITE_API_BASE_URL=

# 开发代理目标（仅本地开发使用）
VITE_PROXY_TARGET=http://localhost:8080